<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperar Código de Acesso</title>
    <style>
        /* Estilos para a página */
        body {
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #404040; /* Fundo cinzento */
            font-family: Arial, sans-serif;
        }

        /* Estilos para o formulário */
        .form-container {
            background-color: #ffffff; /* Fundo branco */
            max-width: 500px;
            width: 100%; /* Para ocupar a largura máxima até 500px */
            padding: 40px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
            border-radius: 5px;
            text-align: center;
            box-sizing: border-box; /* Garante que padding não afete a largura total */
            margin: auto; /* Garante que o elemento está centralizado horizontalmente */
            position: relative; /* Necessário para o botão ficar posicionado dentro */
        }

            .form-container h2 {
                font-size: 18px;
                margin-bottom: 20px;
                color: #333333;
            }

            .form-container input[type="email"] {
                width: 100%;
                padding: 10px;
                margin-bottom: 15px;
                border: 1px solid #ccc;
                border-radius: 3px;
                box-sizing: border-box;
            }

            .form-container button.recuperar {
                width: 100%;
                padding: 10px;
                background-color: #333333; /* Cor do botão */
                color: #ffffff;
                border: none;
                border-radius: 3px;
                cursor: pointer;
                font-size: 14px;
                font-weight: bold;
            }

                .form-container button.recuperar:hover {
                    background-color: #555555; /* Cor do botão ao passar o mouse */
                }

            /* Estilos para o botão de voltar */
            .form-container .btn-primary {
                position: absolute;
                top: 10px;
                left: 10px;
                background-color: #007bff;
                border: none;
                padding: 10px 15px;
                font-size: 16px;
                cursor: pointer;
                border-radius: 5px;
            }

                .form-container .btn-primary:hover {
                    background-color: #0056b3;
                }
    </style>
</head>
<body>
    <div class="form-container">
        <!-- Botão de voltar posicionado no canto superior esquerdo -->
        <button class="btn-primary" onclick="window.location.href='/Identity/Account/Login'">&lt;</button>

        <h2>Recuperar Código de Acesso</h2>

        <form asp-action="RecuperarCodigoAcessoEmail" method="post" onsubmit="alterarTextoBotao()">
            <div>
                <input type="email" id="email" name="email" required placeholder="Digite seu e-mail" />
            </div>
            <div>
                <button type="submit" class="recuperar" id="botaoRecuperar">Recuperar Código de Acesso</button>
            </div>
        </form>
    </div>
</body>
</html>

<script>
    function alterarTextoBotao() {
        var botao = document.getElementById("botaoRecuperar");
        botao.innerHTML = "Enviando..."; // Altera o texto do botão
    }
</script>