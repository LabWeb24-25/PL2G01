<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Banir Utilizador</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #404040;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center; /* Centraliza verticalmente */
            justify-content: center; /* Centraliza horizontalmente */
            height: 100vh; /* Faz o conteúdo ocupar a altura total da viewport */
        }

        .container {
            max-width: 500px;
            width: 100%;
            background: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .image {
            height: 70px;
            width: 70px;
            display: block;
            margin: 0 auto;
        }

        .warning {
            font-size: 1.2em;
            color: #d9534f;
            margin-bottom: 20px;
            line-height: 1.5; /* Aumenta o espaçamento entre as linhas */
        }

        .form-group1 {
            text-align: center; /* Centraliza horizontalmente o texto */
            line-height: 1.5; /* Aumenta o espaçamento entre as linhas */
        }

        .form-group2 {
            margin-bottom: 15px;
            text-align: left; /* Alinha o conteúdo do grupo à esquerda */
            line-height: 1.5; /* Aumenta o espaçamento entre as linhas */
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            line-height: 1.5; /* Aumenta o espaçamento entre as linhas */
        }

        .readonly-box {
            width: 100%;
            height: auto;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            background-color: #f5f5f5;
            color: #555;
            overflow-y: auto;
            resize: none;
            text-align: left;
            line-height: 1.5; /* Aumenta o espaçamento entre as linhas */
        }

        a {
            color: #007bff;
            text-decoration: none;
            font-size: 0.9em;
            display: block;
            margin-top: 10px;
            line-height: 1.5; /* Aumenta o espaçamento entre as linhas */
        }

            a:hover {
                text-decoration: underline;
            }

        button {
            width: 100%;
            padding: 10px;
            background-color: #d9534f;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            line-height: 1.5; /* Aumenta o espaçamento entre as linhas */
        }

            button:hover {
                background-color: #404040;
            }
    </style>
</head>

<body>

    <div class="container">
        <label for="data-bloqueio"><br></br></label>
        <img class="image" src="~/img/Aloquete.png" />
        <p class="warning">Banimento de Utilizador<br></br></p>

        <!-- Data de bloqueio -->
        <div class="form-group1">
            <label for="data-bloqueio">Data de Bloqueio: 14 de novembro de 2024<br></br></label>
        </div>

        <!-- Motivo do bloqueio -->
        <div class="form-group2">
            <label for="reason">Motivo de Bloqueio:</label>
            <div id="reason" class="readonly-box">Violação dos termos de uso da plataforma.</div>
            <label for="data-bloqueio"><br></br></label>
        </div>

        <!-- Link de suporte -->
        <a href="mailto:suporte@biblioteca_fixe.com">Entre em contacto com o suporte para mais informações<br></br></a>

        <button onclick="voltarPaginaPrincipal()">Voltar para a página principal</button>
    </div>

    <script>
        function voltarPaginaPrincipal() {
            const reason = document.getElementById('reason').innerText;
            if (reason.trim() === '') {
                alert('Por favor, insira um motivo para o banimento.');
            } else {
                alert('Voltando para a página principal...');
            }
        }
    </script>

</body>
</html>
