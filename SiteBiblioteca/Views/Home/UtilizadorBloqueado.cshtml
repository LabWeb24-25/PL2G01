<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Banir Utilizador</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #404040;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center; /* Centraliza verticalmente */
            justify-content: center; /* Centraliza horizontalmente */
            height: 100vh; /* Faz o conteúdo ocupar a altura total da viewport */
        }

        .container {
            max-width: 500px;
            width: 100%;
            background: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .image {
            height: 70px;
            width: 70px;
            display: block;
            margin: 0 auto;
        }

        .warning {
            font-size: 1.2em;
            color: #d9534f;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        .readonly-box {
            width: 100%;
            height: 100px;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            background-color: #f5f5f5;
            color: #555;
            overflow-y: auto;
            resize: none;
            text-align: left;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #d9534f;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
        }

            button:hover {
                background-color: #404040;
            }
    </style>
</head>

<body>

    <div class="container">
        <img class="image" src="~/img/Aloquete.png" />
        <p class="warning">Banimento de Utilizador</p>

        <div class="form-group">
            <label for="reason">Motivo do Banimento:</label>
            <div id="reason" class="readonly-box">Violação dos termos de uso da plataforma.</div>
        </div>

        <button onclick="voltarPaginaPrincipal()">Voltar para a página principal</button>
    </div>

    <script>
        function voltarPaginaPrincipal() {
            const reason = document.getElementById('reason').innerText;
            if (reason.trim() === '') {
                alert('Por favor, insira um motivo para o banimento.');
            } else {
                // Aqui adiciona a navegação para a página principal ou anterior, por exemplo:
                // window.location.href = 'pagina_principal.html';
                alert('Voltando para a página principal...');
            }
        }
    </script>

</body>
</html>
